import Loading from "./Images/loaders.gif";

const Loader = () => {
  return (
    <img
      style={{ marginTop: "15%", marginLeft: "33%" }}
      src={Loading}
      alt="loading..."
    />
  );
};

export default Loader;
